ecommerce_analytics:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "../warehouse.duckdb"
      schema: main
    ci:
      type: duckdb
      path: "../warehouse.duckdb"
      schema: main
